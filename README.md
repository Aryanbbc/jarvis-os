# jarvis-os

Local AI OS with voice input, local transcription, skill routing, vault memory, Kokoro TTS, FastAPI dashboard API, and V.A.U.L.T. React dashboard.

---

## 1. Setup

Run this once from the project root:

```bash
chmod +x setup.sh
./setup.sh